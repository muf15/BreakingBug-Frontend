// src/components/ButtonHaver.jsx
import React from 'react';
import { Button } from '@mui/material';

const ButtonHaver = ({ row }) => {
  return (
    <Button variant="contained" color="primary">
      {/* Example content using row data */}
      Action
    </Button>
  );
};

export default ButtonHaver;
